meta {
  name: Lock
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/wallet/lock
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
