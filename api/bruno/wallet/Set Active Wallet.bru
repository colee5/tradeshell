meta {
  name: Set Active Wallet
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/wallet/active
  body: json
  auth: none
}

body:json {
  {
    "address": "{{walletAddress}}"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
