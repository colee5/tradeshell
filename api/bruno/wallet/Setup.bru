meta {
  name: Setup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/wallet/setup
  body: json
  auth: none
}

body:json {
  {
    "password": "{{password}}"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
