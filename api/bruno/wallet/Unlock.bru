meta {
  name: Unlock
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/wallet/unlock
  body: json
  auth: none
}

body:json {
  {
    "password": "{{password}}"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
